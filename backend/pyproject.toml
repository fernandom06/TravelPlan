[project]
name = "backend"
version = "0.1.0"
description = "Travel planning API."
readme = "README.md"
authors = [
    { name = "Fernando Muñoz Paredes", email = "fernandom06@hotmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.141.1",
]

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
